/**
 * TypeScript definitions for AppFooter component
 */

import { ReactNode } from 'react';

export interface AppFooterProps {
  children?: ReactNode;
}

export default function AppFooter(props: AppFooterProps): JSX.Element;
